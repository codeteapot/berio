# TODO

Any version.

## Track based octet output

Same output for all tree.

## Complete coverage

Try unit testing where `bertree` is not a good solution.

## Print implementation of `bertree`

Primitive with defined length avoids output buffer requirement.

## Make clean documentation

Doing `make clean` at `doc` results only in

```
rm -rf .libs _libs
rm -f *.lo
```
